# 定州市第八中学 · 假期安全提醒每日自动发送

> 用 Python + wxauto 控制 PC 版微信，每天 7:30 自动将安全提醒发到班级家长群，并截图转发到班主任工作群。

---

## 一、运行环境要求

| 项 | 要求 |
|---|---|
| 操作系统 | Windows 10 / 11（64 位） |
| Python | **官方版 3.11**（wxauto4 免费版要求 3.9-3.12；**3.13/3.14 不支持**；微软商店版不可用，详见 [setup_python.md](setup_python.md)） |
| wxauto 库 | **wxauto4**（注意包名带 4，不是 `wxauto`） |
| 微信版本 | 微信 PC 版 4.1.x（推荐 4.1.8.107；4.1.9+ 需 Plus 版）|
| 网络 | 微信能正常登录收发消息 |

> **重要变更**：
> - **包名变了**：官方 PyPI 上的包名是 `wxauto4`（不是 `wxauto`）。直接 `pip install wxauto` 会报 "No matching distribution found"。
> - **微信版本要求变了**：需要微信 PC 版 4.1.x（**3.x 版本不支持**）。可从官方文档提供的 [GitHub Releases 归档](https://github.com/SiverKing/wechat4.0-windows-versions/releases) 下载历史版本。
> - **Python 3.13/3.14 不支持**，必须用 3.9-3.12。推荐 3.11。

> **如果您电脑上是微软商店版 Python 或 3.13/3.14**：请先按 [setup_python.md](setup_python.md) 安装官方版 Python 3.11。

---

## 二、文件清单

```
wechat-safety-reminder/
├── send_safety_reminder.py   ← 核心脚本（含文案、发送、截图、转发、重试、日志）
├── install_deps.bat         ← 依赖安装脚本（双击运行一次即可，自动诊断 Python 来源）
├── run_daily.bat             ← 启动器（双击测试 / 任务计划程序调用，顶部可配置 Python 路径）
├── setup_python.md           ← Python 环境诊断与官方版安装指南（必读）
└── README.md                 ← 本说明文件
```

---

## 三、首次使用 · 配置步骤

### 第 1 步：把整个文件夹放到电脑上

建议放在 **`D:\safety_reminder\`**（脚本默认路径）。如果换成其他盘，需要同时修改 `send_safety_reminder.py` 顶部的 `BASE_DIR`。

### 第 2 步：检查 Python 环境

**强烈建议先按 [setup_python.md](setup_python.md) 诊断 Python 环境。**

简而言之：
- 如果是微软商店版 Python 或 Python 3.14 → 必须额外装官方版 Python 3.11
- 如果已经是官方版 Python 3.11 → 可直接继续

### 第 3 步：安装依赖

双击运行 **`install_deps.bat`**，脚本会自动诊断 Python 来源并安装 `wxauto4`（注意是 wxauto4，不是 wxauto）。

如果检测出商店版或 3.13/3.14，按提示装好官方版 3.11 后，手动执行以下命令安装依赖（注意带 `py -3.11 -m`）：

```bat
py -3.11 -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple --upgrade wxauto4
```

> 💡 `-i https://pypi.tuna.tsinghua.edu.cn/simple` 是清华镜像源，国内访问速度更快、避免连接 PyPI 失败。如需永久配置，详见 [setup_python.md 第五节](setup_python.md)。
>
> ⚠ **包名是 `wxauto4`，不是 `wxauto`**。wxauto4 会自动安装 pywin32、pillow、psutil 等依赖，无需单独安装。

### 第 4 步：登录微信

1. 打开 PC 版微信（**需 4.1.x 版本，3.x 不支持**）并扫码登录。
2. **保持微信窗口可见**（不能最小化到任务栏；可以缩到角落）。
3. 确保在最近聊天列表里能看到目标群（**先用手机微信进入一次群聊**，PC 端就会自动出现）。

> 如需安装 4.1.8.107 历史版本：https://github.com/SiverKing/wechat4.0-windows-versions/releases/tag/v4.1.8.107
> （先卸载当前微信，再装这个版本；安装后建议关闭自动更新避免被升级到 4.1.9+）

### 第 5 步：先跑一次测试模式

打开 `send_safety_reminder.py`，确认顶部配置区：

```python
TEST_MODE = True   # ← 保持 True，消息只发到「文件传输助手」，安全测试
```

双击运行 `run_daily.bat`。如果一切正常：
- 微信会自动跳到「文件传输助手」并发送文案
- 自动截一张图保存到 `D:\safety_reminder\screenshots\`
- 自动把截图也发回「文件传输助手」

测试通过后，把 `TEST_MODE` 改为 `False`，并将以下两个群名改成实际名称：

```python
PARENT_GROUP_NAME = "定州八中2024级3班家长群"   # 你的实际家长群名（一字不差）
WORK_GROUP_NAME   = "定州八中班主任工作群"        # 你的实际工作群名
```

> ⚠ **群名必须一字不差**，包括空格、标点、emoji。最稳妥的方式：从微信里复制群名。

### 第 6 步：再次手动跑一次正式模式

确认文案能正确发到家长群、截图能转发到工作群。

---

## 四、设置 Windows 任务计划程序（每天 7:30 自动执行）

1. 按 `Win + R`，输入 `taskschd.msc`，回车打开「任务计划程序」。
2. 右侧点击 **「创建基本任务」**。
3. 名称填：`定州八中每日安全提醒`，下一步。
4. 触发器选 **「每天」**，下一步。
5. 开始时间设为 **`07:30:00`**，下一步。
6. 操作选 **「启动程序」**，下一步。
7. 「程序或脚本」一栏浏览选择 `D:\safety_reminder\run_daily.bat`（或你实际放置的路径）。
8. 「起始于（可选）」一栏填写 `D:\safety_reminder\`（**注意末尾带反斜杠**）。
9. 下一步 → 完成。
10. 在任务列表里找到刚建的任务，**右键 → 属性**，做以下强化：
    - 「条件」标签页：取消勾选「只有在计算机使用交流电源时才启动此任务」（笔记本必做）。
    - 「设置」标签页：勾选「如果任务失败，按以下频率重启」，建议间隔 5 分钟，最多 3 次。
    - 「常规」标签页：勾选「不管用户是否登录都要运行」（**注意：选这个的话 Windows 会以隐藏窗口方式运行，wxauto 可能因 UI 不可见失败，建议保持不勾选**）。
    - 「常规」标签页：勾选「使用最高权限运行」。
    - 「触发器」标签页 → 编辑 → 勾选「唤醒计算机运行此任务」（可选，针对休眠场景）。

### 关于「电脑不一定 24h 开机」的容错方案

- 如果每天 7:30 电脑处于关机或睡眠状态，**Windows 任务计划程序支持「错过后尽快运行」**。
- 配置位置：任务属性 → 「设置」标签页 → 勾选 **「如果计划任务错过预定启动时间，尽快启动任务」**。
- 这样即便开机时间晚于 7:30（例如 8:00 开机），系统也会在登录后立即补发，避免漏发。

---

## 五、运行日志查看

- 日志文件路径：`D:\safety_reminder\send.log`
- 日志示例：
  ```
  2026-07-25 07:30:03 [INFO] ====== 开始执行每日安全提醒任务 [正式模式] ======
  2026-07-25 07:30:03 [INFO] 第 1/3 次尝试 ...
  2026-07-25 07:30:05 [INFO] ✓ 微信已连接
  2026-07-25 07:30:08 [INFO] ✓ 安全提醒已发送至【XX班家长群】
  2026-07-25 07:30:13 [INFO] ✓ 截图已保存：D:\safety_reminder\screenshots\reminder_20260725_073005.png
  2026-07-25 07:30:16 [INFO] ✓ 截图已转发至【班主任工作群】
  2026-07-25 07:30:16 [INFO] ✓✓✓ 任务执行成功 ✓✓✓
  ```

---

## 六、常见问题排查

### Q1：脚本提示"连接微信失败"

- 微信窗口是否被最小化？→ 还原窗口。
- 是否运行了多个微信实例？→ 只保留一个。
- 微信是否登录了？→ 重新扫码登录。
- 微信版本是否为 4.1.x？wxauto4 不支持 3.x 微信。下载 4.1.8.107：https://github.com/SiverKing/wechat4.0-windows-versions/releases/tag/v4.1.8.107
- 是否在远程桌面 / 锁屏状态？wxauto4 依赖 UI 元素，**建议电脑保持用户登录且非锁屏状态**。Windows 任务计划程序属性里取消「不管用户是否登录都要运行」。

### Q2：发送时提示找不到群

- 群名是否一字不差？包含空格、emoji、特殊字符都要对齐。
- 该群是否在微信的「最近聊天」列表中？**用手机微信进入该群一次**，PC 端会自动同步。
- wxauto4 默认 `exact=False`（模糊匹配），如果同名群较多，可在脚本中改为精确匹配。

### Q3：截图为空或截图区域不对

- wxauto4 免费版没有内置 Screenshot 方法，本脚本使用 `pywin32` 的 PrintWindow API 直接截取微信窗口（类名 `WeChatMainWndForPC`）。
- 如果截图全黑：可能微信窗口被遮挡或处于最小化状态。脚本会自动尝试还原窗口，但仍建议手动保持微信窗口可见。
- 截图全黑且窗口可见：可能是 GPU 渲染兼容性问题，可改用 `pyautogui.screenshot()` 全屏截图方案（替换 `screenshot_wechat_window` 函数实现）。

### Q4：发到工作群的截图打不开 / 体积为 0

- 检查 `D:\safety_reminder\screenshots\` 目录权限，确保当前用户可读写。
- 关闭各类杀毒软件对 D 盘的写入限制。

### Q5：任务计划程序跑了但没发消息

- 任务属性里是否勾选了「不管用户是否登录都要运行」？**取消勾选**，改为只在登录时运行。
- 任务属性 →「条件」→ 取消「只有在计算机使用交流电源时才启动此任务」。

---

## 七、安全与合规提示

1. wxauto4 通过模拟用户操作控制微信 PC 版 4.1.x，**不修改微信本体、不读取聊天记录**，安全性较好。但仍属第三方工具，存在极小概率被微信风控的可能性。
2. **建议每天只发送一次**，不要频繁触发，避免被识别为营销机器人。
3. 若长期使用过程中收到微信安全提示，建议暂停几天后再次启用，或改用「企业微信群机器人」官方接口（无任何封号风险，需要学校开通企业微信）。
4. 文案中的日期由脚本 `datetime.now()` 自动取当天日期，**无需人工修改**，跨年也能继续使用。
5. wxauto4 免费版要求微信 4.1.8.107；如果被微信自动升级到 4.1.9+，需要购买 Plus 版 (`pip install wxautox4`)，或手动安装 [4.1.8.107 历史版本](https://github.com/SiverKing/wechat4.0-windows-versions/releases/tag/v4.1.8.107)。

---

## 八、后续可扩展

- **节假日跳过**：在 `main()` 函数顶部加入判断，如周末、寒假、春节等自动跳过。
- **多班级并行**：把 `PARENT_GROUP_NAME` 改成列表，循环发送。
- **内容日历**：用 Excel 维护每日文案，脚本读取当日内容发送，便于轮换防溺水 / 防诈骗 / 防火等专题。
- **企业微信官方 API**：若学校已开通企业微信，可改用企业微信群机器人 webhook，**零风险、跨平台、无需电脑开机**。这是长期最稳的方案。

---

如需上述任一扩展功能，告诉我即可帮您改造脚本。
